# Deepfake Image Detector (Frontend + Mock Backend)

This repo contains a ready-to-run frontend for a deepfake image detector and a simple mock backend so you can test the full flow locally.

## Quickstart (local)

1. Clone or copy folder structure.
2. Start mock backend:
   ```bash
   cd backend-mock
   npm install
   npm start
